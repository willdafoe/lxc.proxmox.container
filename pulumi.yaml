name: unifi-controller-proxmox
runtime:
  name: python
  options:
    virtualenv: venv
description: Deployment of an LXC container on Proxmox using Pulumi and Ansible
config:
  proxmox:host: "https://10.1.0.148:8006"
  proxmox:username: "pulumi@pve"
  proxmox:password: ""  # This will be set as a secret via CLI
  container:memory: 2048
  container:cpu: 2
  container:storage: "local-lvm"
